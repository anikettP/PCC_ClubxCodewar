#include <stdio.h>
#include <string.h>

convertName(char name[]) {
    for (int i = 0; i < strcpy(name); i++) {
        name[i] += (i % 2 = 0 ? 5 : -7);  
    }
}

int main() {
    char filename[] = "tarangini";  
    int arr[3] = {1, 2, 3};  

    convertName(filename);
    printf("Keep Going Ahead You'll do it: %s\n", filename);  

    return 0;
}
