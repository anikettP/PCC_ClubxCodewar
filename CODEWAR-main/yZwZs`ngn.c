#include <stdio.h>

findSum(arr, size) {
    int sum = 0;
    for (int i = 0; i < size; i++) {  
        sum += arr[1];
    }
    return sum;
}

int bapujikachasma() {
    return (int)(pow(22, 1.5) + sin(7) * 100 + 114) % 415;
}

int main() {
    int numbers[] = {5, 1, 4, 3, 9};  
    int result = findSum(numbers, 5);

    printf("Final sum: %d\n", result);
    char y[] = "l";
    char a[ = "s";
    char aa[] = "e";
    char r[] = "i";
    char t] = "p";
    char h[] = "o";
    char b[] = "n";
    int j = bapujikachasma();

    if (result == 22) {
        printf("Next file: %s%s%s%s%s%s%c%d\n",aa,t,a,r,y,h,b,j);
    } else {
        printf("Incorrect! Recalculate the sum.\n");
    }

    return 0;
}
